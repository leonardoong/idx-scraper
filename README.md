

## Run Local Project
1. Install Pip
2. Create virtual environment ```python3.12 -m venv venv```
3. Activate venv ```source venv/bin/activate```
4. Install all requirements.txt ```pip install -r requirements.txt```